<local:BaseItemView x:Name="userControl"
    x:Class="Islands.UWP.ReplyView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Islands.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <local:BaseItemView.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="IsTextSelectionEnabled" Value="True"/>
        </Style>
    </local:BaseItemView.Resources>
    <local:BaseItemView.BottomContent>
        <Line Fill="Gray" Stroke="Gray" Height="1" X2="{Binding ActualWidth, ElementName=userControl,Mode=OneWay}"/>
    </local:BaseItemView.BottomContent>
    <Grid Margin="0,0,0,5">
        <StackPanel Name="threadSp" Margin="2">
            <RelativePanel>
                <TextBlock Name="title" Text="{x:Bind ItemTitle}"
                           Visibility="{x:Bind IsHadTitle}" FontSize="{Binding TitleFontSize}"/>
                <TextBlock Name="email" Text="{x:Bind ItemEmail}"
                           Visibility="{x:Bind IsHadEmail}"
                           RelativePanel.Below="title" FontSize="{Binding TitleFontSize}"/>
                <TextBlock Name="name" Text="{x:Bind ItemName}"
                           Visibility="{x:Bind IsHadName}"
                           RelativePanel.Below="email" FontSize="{Binding TitleFontSize}"/>
                <TextBlock Name="txtCreateDate" Text="{x:Bind ItemCreateDate}"
                           RelativePanel.Below="name" FontSize="{Binding TitleFontSize}"/>
                <TextBlock Name="id" Text="No."
                           RelativePanel.LeftOf="txtId" FontSize="{Binding TitleFontSize}"/>
                <TextBlock Name="txtId" Text="{x:Bind ItemNo}" 
                           RelativePanel.AlignRightWithPanel="True" FontSize="{Binding TitleFontSize}"/>
                <TextBlock Name="txtUserid" Text="{x:Bind ItemUid}"
                           RelativePanel.Below="txtCreateDate" FontSize="{Binding TitleFontSize}"/>
            </RelativePanel>
            <ContentControl Name="txtContent" Content="{x:Bind replyContent}"/>
        </StackPanel>
    </Grid>
</local:BaseItemView>
